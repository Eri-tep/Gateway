{
  "name": "Wallpad Profile",
  "attributes": {
    "profile": {
      "schema": {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "enum": ["auto", "custom1", "custom2", "custom3"]
          }
        },
        "additionalProperties": false,
        "required": ["value"]
      }
    }
  },
  "commands": {
    "setProfile": {
      "arguments": [
        {
          "name": "profile",
          "schema": {
            "type": "string",
            "enum": ["auto", "custom1", "custom2", "custom3"]
          }
        }
      ]
    }
  }
}
